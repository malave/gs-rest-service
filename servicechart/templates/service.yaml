apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name }}-service
  labels:
{{ include "app.labels" . | indent 4 }}
spec:
  selector:
    app: {{ .Values.name }}
    tier: {{ .Values.network.tier }}
  ports:
    - protocol: TCP
      port: {{ .Values.port }}
